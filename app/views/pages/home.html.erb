<h3>Latest pictures</h3>

<div class="photo-grid">
  <% @latest_photos.each do |photo| %>
    <%= link_to item_path(photo)  do %>
      <%= image_tag photo.image.medium %>
    <% end %>
  <% end %>
</div>
